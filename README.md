# mes-series

Application personnelle de suivi de series et de films. Version en ligne : https://astoul1512-lang.github.io/mes-series/
Version en production : v114 — **l'IA range les rangées de Découvrir, et en écarte ce qui jure avec le profil** (SPEC-09 lot 1, première moitié). Décision d'Adrien du 31/08 : « j'aimerais quand même que l'IA check ça pour être sûr de la cohérence avec le profil : Acclamés, Pépites, Week-end, Classiques, Incontournables. » Ces cinq rangées gardent leur SOURCE LOCALE — leurs requêtes TMDB sont objectives et mesurées, et c'est ce qui fait qu'« Acclamés par la critique » reste VRAI. L'IA n'y fait que deux choses : ranger, et retirer ce qui jure franchement. La nouvelle tâche `ordonner_rangee` ne rend que des NUMÉROS, jamais des titres : le modèle n'a aucun champ pour ajouter quoi que ce soit, donc la première borne de la spec — « le contrôle n'invente rien » — est tenue par construction et non par vigilance. **« Bientôt » n'est ni rangée, ni contrôlée, ni écartée**, et la garde est écrite DEUX fois exprès (à la liste des rangées éligibles et à l'endroit où l'ordre s'applique) : c'est un calendrier, l'ordre EST l'information, et un titre retiré d'un calendrier est une sortie qu'on ne verra pas venir. « Vu par tes proches » et « Nouveautés » sont rangées mais jamais écartées — un fait social ne se cache pas, et une nouveauté écartée serait une nouveauté cachée. La contradiction que la spec portait a été tranchée avec Adrien : si l'IA écarte quatre titres d'une rangée de douze, avec quoi complète-t-on ? Reprendre dans le tas remettrait les écartés, descendre plus bas dans TMDB ramènerait des titres que l'IA n'a jamais vus — une rangée à moitié contrôlée, présentée comme contrôlée, ce qui est pire que rien. **On lui en soumet donc vingt pour n'en afficher que dix** (« on n'a qu'à faire une requête plus importante à TMDB ») : les remplaçants ont été vus et rangés. Mesuré : 21 appels TMDB IA allumée contre 18 IA éteinte, soit **trois de plus**, et zéro quand l'IA est coupée — le socle ne paie pas pour une fonctionnalité qu'il n'a pas. Trois garde-fous tiennent le reste : le plafond de 40 % d'écartés (au-delà, l'IA ne contrôle plus, elle recompose — les écartés en trop reviennent), la règle des 10 qui prime toujours (sous le plancher, les derniers écartés reviennent, une rangée n'est jamais maigre ni supprimée), et l'échec qui rend l'ordre local sans un message. Le calcul se fait dans le lot du jour, APRÈS le premier rendu ; la lecture à l'affichage est SYNCHRONE et ne parle jamais au réseau — rien ne bouge sous le doigt. Les places se prennent dans le compteur des pitchs, pas dans un compteur privé : c'est le dernier frein journalier depuis que le plafond serveur par utilisateur a disparu, et lui en donner un second reviendrait à le contourner ; par-dessus v113 — **une notification quand l'IA tombe, et une seule par panne.** Le même jour, Adrien a décidé de SUPPRIMER le plafond par utilisateur : sa raison tient — le repli local existe partout, donc une panne de quota dégrade sans casser, et `CLAUDE.md` en fait une règle non négociable. Mon objection reste écrite dans `config.ts` parce qu'elle reste vraie : l'échelle des fournisseurs protège contre un fournisseur EN PANNE, pas contre l'app qui APPELLE TROP ; les quotas amont étant PARTAGÉS, une boucle sur un seul appareil fait tomber TOUS les comptes en dégradé, pas seulement le fautif. La contrepartie, c'est ce lot : l'alerte devient le SEUL signal. Elle part quand l'échelle est entièrement épuisée ou quand le budget global du jour est atteint — jamais sur une réponse mal écrite, parce qu'un fournisseur qui répond mal a répondu et que faire sonner un téléphone pour un incident qui n'existe pas, c'est apprendre à ne plus le regarder. **La bascule « ça marchait → ça ne marche plus » est décidée EN SQL**, par un `update … where not en_panne` atomique : deux requêtes simultanées ne peuvent pas envoyer deux notifications pour une seule panne. Adrien avait d'abord demandé « aucune limite de fréquence » ; une panne de six heures aurait alors envoyé des dizaines de notifications, ce qui est la façon la plus sûre de faire couper les notifications d'une app pour de bon — il a tranché « je veux juste une notif », et c'est cette phrase que la migration 018 rend vraie. **Deux textes, parce qu'il y a deux pannes** : « ⚠️ Quota IA atteint · Plus de requêtes jusqu'à demain » quand c'est nous qui avons tout consommé, « ⚠️ IA injoignable · Aucun fournisseur ne répond » quand c'est Google ou OpenRouter qui ne répond pas. Annoncer l'une pour l'autre serait faux une fois sur deux, et une alerte fausse ne se croit plus ; on ne dit « quota » que si TOUS les refus étaient des saturations. Les deux messages se terminent par « l'app continue sans l'IA », parce qu'un ⚠️ reçu à 23 h laisse croire que tout est mort alors que rien ne l'est. Elles portent le même tag, donc une seconde alerte REMPLACE la première sur l'iPhone au lieu de s'empiler. Rien de tout ça ne coûte une milliseconde à une requête qui réussit : la fermeture de l'incident part en tâche de fond (`EdgeRuntime.waitUntil`), et `npm:web-push` n'est importé qu'au moment où une alerte part vraiment — jamais au démarrage de l'instance, qui est justement le chemin que la v112 vient d'accélérer. Le point de défaillance silencieux est écrit noir sur blanc dans `INSTALL.md` §9 : si le compte visé n'a aucun appareil abonné, l'alerte ne partira JAMAIS et rien ne le dira — d'où les trois requêtes SQL qui permettent de l'éprouver sans attendre une vraie panne. Aucun fichier servi ne change ; par-dessus v112 — **la recherche ✦ démarre sur le petit modèle** (RETOUR-10 §1). Mesuré dans `ia_journal` le 31/08 sur la tâche `interpreter_recherche` : `gemini-flash` rend la main en 3 983 ms de médiane (2 910 → 7 895) et tombe deux fois sur dix en délai dépassé — 8 096 ms perdus avant de basculer — tandis que `gemini-flash-lite` répond en 949 ms avec une régularité de métronome (945 → 953). Pire cas d'alors : NEUF SECONDES pour une phrase simple. Découper « un film d'action avec Will Smith » en critères est de l'ANALYSE DE TEXTE, pas de la culture générale : la tâche part désormais du petit modèle et ne remonte au fort QUE si la réponse ne vaut rien — réponse illisible, ou mode « titre » sans aucun candidat exploitable, c'est-à-dire le cas où il faut de la mémoire pour reconnaître un film à sa description. C'est une **exception assumée à RETOUR-01 point 4** (« toutes les tâches démarrent à l'étage 1, pertinence d'abord ») : cet arbitrage-là tenait parce qu'on ne pouvait pas RATTRAPER un petit modèle qui se trompe. On le peut maintenant, donc on ne renonce pas à la qualité, on renonce à la PAYER quand elle ne sert pas. Les huit autres tâches gardent l'échelle d'origine, et un test le vérifie. Un chemin qu'il aurait été facile d'oublier est traité : quand les petits étages sont saturés et que PERSONNE n'a répondu, la requête monte quand même au modèle fort — sans quoi partir du rang 3 aurait retiré pour de bon les deux meilleurs étages, et un lot de VITESSE aurait coûté de la DISPONIBILITÉ. Ce chemin-là ne coûte pas de seconde unité de budget, parce qu'aucune première réponse n'a été obtenue ; l'escalade au sens de la spec, elle, en prend une seconde, et si elle est refusée on dégrade sans un mot. Jamais deux modèles en parallèle, jamais deux escalades, jamais un étage rejoué : les deux appels sont tracés dans `ia_journal`, ce qui rend le taux d'escalade mesurable après coup (la requête est dans `INSTALL.md`, avec le seuil de ~40 % au-delà duquel il faudra revenir en arrière). À l'écran, le troisième état de la maquette 28 est enfin déclenché : « ✦ Je cherche plus loin… · ta description demande de la mémoire ». **Il est déduit de la durée, et c'est un choix d'Adrien pris en connaissance de cause** — l'escalade se passe entièrement sur le serveur, qui ne parle au téléphone qu'une fois, à la fin ; passé 1 400 ms (contre 949 ms de médiane mesurée), le petit modèle a presque toujours déjà rendu la main. Un jour où il ramera, la ligne s'affichera sans qu'il y ait eu de second essai : c'est la RAISON affichée qui peut être fausse, jamais l'attente, qui elle est bien réelle. La réponse porte `escalade:true` quand l'escalade a eu lieu pour de vrai, ce qui permet de mesurer l'écart au lieu de le supposer. La minuterie est désarmée par tout changement d'état, et un test vérifie qu'aucune ligne ne réapparaît APRÈS les résultats — ce qui serait pire que de ne rien montrer ; par-dessus v111 — **deux clés Gemini : le quota gratuit est réellement doublé.** L'échelle des fournisseurs de la fonction Edge `ia` passe de trois étages à cinq : `gemini-flash` et `gemini-flash-lite` apparaissent chacun DEUX fois, sur deux comptes Google différents. Le point qui bloquait était minuscule et décisif — le relais DÉDUISAIT la clé du nom du fournisseur (`gemini…` → `GEMINI_API_KEY`), si bien que deux étages Gemini ne pouvaient matériellement pas porter deux clés. Un champ `cle_env` sépare désormais les deux choses que ce nom disait à la fois : le COMPTE qu'on débite (la colonne) et le DIALECTE qu'on parle (le nom, `generateContent` ou le dialecte OpenAI). **La condition qui rend le lot vrai a été vérifiée avant d'écrire une ligne** : le quota gratuit de Gemini se compte par PROJET Google Cloud, pas par clé — deux clés du même projet auraient partagé le même compteur, la bascule n'aurait rien apporté et aurait juste ajouté un étage qui échoue. Les deux clés d'Adrien viennent de deux projets ET de deux comptes. Les compteurs suivent tout seuls : `ia_compteurs` a pour clé (fournisseur, fenêtre) et le fournisseur est un NOM, donc `gemini-flash-2` et `gemini-flash-lite-2` ont leurs propres lignes sans une fonction SQL de plus — un même nom sur deux comptes les aurait comptés ENSEMBLE, c'est-à-dire aurait annulé le lot en silence. L'ordre est une décision : on épuise un modèle SUR LES DEUX COMPTES avant de descendre en qualité, parce qu'un second compte de Flash vaut mieux qu'un premier compte de Flash-Lite. Deux effets de bord ont été traités dans le même lot, parce qu'ils naissent de lui : **cinq étages à huit secondes feraient quarante secondes d'attente**, donc la requête entière est bornée à 20 s (déjà plus serré que les 24 s d'avant), l'arrêt se lit dans `ia_journal` sous un statut propre, et on borne le TEMPS plutôt que le NOMBRE d'étages — un compteur saturé ou une clé absente se traversent en quelques millisecondes et n'ont aucune raison de fermer l'échelle ; et **un étage sans clé ne prend plus de réservation du tout** au lieu d'en prendre une pour se la faire rembourser, ce qui est l'état ordinaire de deux étages sur cinq tant que `GEMINI_API_KEY2` n'est pas posée. Les deux ordres de déploiement sont sûrs et c'est délibéré : migration avant fonction, les étages neufs tapent sur la clé n° 1 (deux tentatives redondantes, rien de cassé) ; fonction avant migration, les lignes sans `cle_env` retombent sur l'ancienne règle mot pour mot. Sans la seconde clé, les étages 2 et 4 sont sautés, journalisés, et l'app se comporte exactement comme en v110. Aucun fichier servi ne change ; par-dessus v110 — **entretien : les leçons du 01/09 et une porte fermée en amont.** Trois règles entrent dans `CLAUDE.md`, chacune payée dans la journée : *un serveur de test frais avant d'accuser le code* (un `lot D` rouge par intermittence venait du `python3 -m http.server` épuisé après une trentaine de lancements de navigateur — mesuré `main` 7/7, la même branche 1/5 sur le serveur usé puis 3/3 sur un port neuf, fichiers identiques) ; *un lot qui touche au rendu SE REGARDE* (RETOUR-09 passait ses vingt cas et était faux à l'œil — le texte de repli d'une jaquette débordait de son cadre et chevauchait le titre, les tests mesuraient la hauteur et pas le débordement, une capture l'a trouvé en une seconde) ; et *ce qui naît et meurt dans la même frame n'existe pas, ou clignote* (« ✓ Compris » s'affichait zéro milliseconde, et la ligne d'attente aurait scintillé sur une réponse servie par le cache — un scintillement ressemble à une panne, soit l'inverse exact de ce qu'un indicateur d'attente doit produire). Le dépôt gagne aussi son premier `.gitignore` : il n'en avait pas, et la v106 a été consacrée à retirer `CLAUDE.md` d'une publication HTTPS publique où il était arrivé en silence. Le `.DS_Store` que macOS sème était dans la même situation — hors du dépôt par chance, pas par règle, et à un `git add .` près d'être servi. `_config.yml` reste la seconde défense : l'un empêche d'AJOUTER, l'autre empêche de SERVIR. Aucun fichier servi ne change dans cette version ; par-dessus v109 — **la recherche ✦ dit qu'elle cherche** (RETOUR-10 §2, maquette 28 variante A). Demande d'Adrien : « quand je fais une recherche IA ça met un peu de temps ; montrer que l'IA est en train de chercher, sinon on a l'impression que ça bugue. » À la validation en mode ✦, la barre porte un liseré violet qui court sous elle et une ligne de statut apparaît au-dessus de la grille : « ✦ Je lis ta phrase… », puis « ✓ Compris » en vert avec ce qui a été compris en clair (« film · action · Will Smith »). **La grille précédente ne disparaît jamais** — c'est la règle du point 5 / point 7, et le premier réflexe en ajoutant un écran d'attente est justement de vider. Le liseré est posé en touchant la classe du nœud existant, jamais par un repeint de l'en-tête, qui coûterait le focus du champ et refermerait les feuilles ouvertes. Deux garde-fous trouvés en écrivant les tests : **refaire la même phrase n'affiche rien du tout** (le cache répond dans la même frame, poser puis retirer la ligne aurait produit un scintillement — c'est-à-dire l'impression de panne que le lot combat), et **« ✓ Compris » tient 900 ms au minimum** parce qu'il naissait et mourait dans la même frame quand rien ne restait à charger. C'est une exception assumée à une règle écrite du dépôt (« un spinner, c'est une attente, et une attente est un blocage ») : cette règle visait une IA qui ENRICHIT un écran déjà peint ; ici elle est sur le chemin critique, et ne rien montrer ne supprime pas l'attente, ça la fait passer pour une panne. IA coupée : aucun liseré, aucune ligne, comportement de v108 à l'identique. Le §1 du même retour — démarrer sur le petit modèle et n'escalader qu'au besoin — touche la fonction Edge `ia` et sera livré à part, faute de pouvoir exécuter `deno test` ; l'état « ✦ Je cherche plus loin… » existe déjà dans le code et dans les tests, mais rien ne le déclenche encore ; par-dessus v108 — **filtrer la bibliothèque d'un proche** (RETOUR-11). La bibliothèque de quelqu'un d'autre affichait quatre blocs figés — En cours, Séries vues, Films vus, Sa liste à voir — sans recherche, sans tri, sans filtres. Elle porte désormais EXACTEMENT les mêmes commandes que Mon profil : les puces de famille (Séries · Animés · Films · À voir · En pause) et la barre Chercher · Trier · Filtrer, avec les mêmes composants et le même dessin — pas une seconde façon de filtrer une bibliothèque. Le vrai risque du lot était invisible : `pf12`, l'état des filtres, était un objet GLOBAL unique, et le réutiliser tel quel aurait fait que les filtres posés chez Marie s'appliquaient à MA bibliothèque au retour — des titres qui manquent, sans le moindre message. Il devient un pointeur sur un registre par bibliothèque (`moi`, `proche:<id>`), si bien qu'un aller-retour conserve les filtres de chacun et qu'ouvrir la bibliothèque d'un AUTRE proche repart d'un état propre. Les cinq points de navigation codés en dur sur `view === 'profile'` sont étendus à `biblio` — sans quoi le bouton retour d'Android aurait quitté l'écran au lieu de fermer la recherche, exactement ce que la carte de la pile interdit. Le filtre « Jamais notés » ne s'affiche PAS chez un proche : il interroge `db.avis`, MA base, et aurait répondu « ce que MOI je n'ai pas jugé », une réponse fausse donnée avec assurance ; ses avis à lui voyagent pourtant dans `biblios[id].avis`, mais aucun code ne les lit encore — c'est un lot à part. Lecture seule inchangée : le bandeau reste, les cartes gardent leurs gestes actuels, aucun geste de ma bibliothèque n'est proposé sur ses titres, et un identifiant non numérique n'est jamais rendu cliquable. Effet de bord assumé et signalé avant d'être codé : les puces exposent les séries EN PAUSE et les films À VOIR du proche, que les quatre blocs figés cachaient alors que les données étaient là ; par-dessus v107 — **la jaquette dans le centre de notifications** (RETOUR-09). Une sortie s'annonçait avec un simple pictogramme — 📅 pour un épisode, 🎬 pour un film — et une ligne de texte à côté d'un émoji ne dit pas DE QUOI on parle ; l'affiche, si. Les entrées de sortie portent désormais la jaquette verticale du titre (40 × 60, rapport 2/3), lue directement dans `db.shows` / `db.movies` : ces entrées portent déjà `type` et `id`, et une sortie ne concerne qu'un titre suivi, donc l'affiche est déjà en base — **zéro appel d'API supplémentaire, mesuré**. Le film prend bien son `poster` et non le `image` de `filmsBientot`, qui vaut `backdrop || poster` et aurait couché un bandeau paysage dans un cadre 2/3. Un titre sans affiche connue retombe sur le repli natif de `posterEl` — un cadre neutre au début du titre, celui que le reste de l'app emploie partout — et non sur le pictogramme : garder l'émoji aurait rouvert un second chemin d'affichage propre au centre, et surtout fait sauter la hauteur de la ligne d'une entrée à l'autre (84 px avec jaquette contre 59 px avec le carré). Le gabarit repris est `.recocp`, du même écran, et non `.cthumb` que la spec citait — celle-ci fait 74 × 42, c'est la vignette PAYSAGE du calendrier. Rien ne change pour les cartes de reco (elles ont déjà leur affiche) ni pour les retours « X a ajouté … », dont le ✓ porte l'information et qui parlent d'une personne autant que d'un titre ; par-dessus v106 — **`CLAUDE.md` cesse d'être publié en HTTPS public.** Le fichier est arrivé à la racine du dépôt sans passer par la liste d'exclusion de `_config.yml`, et GitHub Pages sert par défaut tout ce qui traîne à la racine : la carte complète de l'architecture, des règles de sécurité, des budgets IA et des pièges du projet était donc lisible par n'importe qui à l'adresse publique. Personne ne l'avait vu — c'est le contrôle « publication » du lanceur de tests qui l'a signalé, et le pavé de commentaire en tête de `_config.yml` avait annoncé l'accident mot pour mot : « un nouveau document interne posé à la racine serait donc publié par défaut, en silence ». Aucun code de l'app ne change ; la version est incrémentée pour que chaque fusion sur `main` garde son numéro propre ; par-dessus v105 — quatre lots d'un coup, livrés ensemble pour relecture : **le compteur ne ment plus depuis le cache** (RETOUR-07 §2) — « 0 titres » et « moins de 0 séries » s'affichaient au-dessus d'une grille pleine sur Tout, Films et Séries dès qu'on refaisait la même recherche, parce que le cache retenait le total PROVISOIRE du premier étage (celui qui ne compte pas) et le resservait figé ; désormais un total n'entre en cache que fini, l'amorçage de fond vient y recoller le chiffre définitif, et un total inconnu s'affiche « Résultats » plutôt que « 0 » ; **un banc d'essai IA caché** (SPEC-09 lot 0) — dans Réglages › Développeur (sept appuis sur le pied de page), l'IA compose elle-même des rangées de Découvrir au service du profil, chaque titre proposé est vérifié sur TMDB avant d'être affiché puis passe le tamis habituel, les jetés sont comptés avec leur raison, et un pouce par rangée s'enregistre et s'exporte — l'écran Découvrir réel ne bouge pas d'un pixel ; **le retour ramène enfin toujours sur l'écran précédent** — cinq écarts trouvés à l'audit et corrigés : le duel ouvert depuis Découvrir ou Mon profil faisait apparaître Mes goûts au retour, l'aperçu d'un titre était écrasé quand on partait sur un autre depuis « Dans le même esprit », une feuille ouverte survivait au changement d'écran et mangeait le premier retour, une fiche dont le titre a été supprimé laissait un « Introuvable » qui bouclait, et « Masquer les outils » du banc empilait les Réglages ; **la barre ✦ comprend les personnes, les descriptions et le « et »** (SPEC-11) — « je cherche un film d'action avec Will Smith » pose la famille, le genre et l'acteur (résolu sur TMDB, visible et retirable), « je cherche le film où Leonardo DiCaprio est courtier et se drogue » propose Le Loup de Wall Street en carte « C'est celui-là ? », « je veux un film d'action et d'aventure » bascule le réglage « Les deux » — une requête IA par validation, et le ✦ retombe sur le comportement d'aujourd'hui dès que l'IA ne répond pas ; par-dessus v104 — RETOUR-08 §2 : la ligne du 💌 sur une fiche de série prend la forme de celle de la fiche film, choisie par Adrien sur maquette. Le carré 💌 n'est JAMAIS seul : il est toujours posé à droite d'un élément qui dit l'état ou porte l'action — « Marquer S…E… comme vu » sur une série en cours, un témoin vert « ✓ Série vue » sur une série finie, « ✓ À jour » sur une série qu'on a rattrapée mais qui continue, « ⏳ Pas encore diffusée » sur une série pas encore sortie, et « Reprendre » sur une série en pause (le carré rejoint la carte plutôt que d'ouvrir une rangée pour lui seul). Le bouton pleine largeur libellé de la v103 est retiré, ainsi que la variante de code qui le produisait — une forme sans appelant se remet en service par accident. Les témoins ne sont pas des boutons : sur le film la barre verte annule « vu », mais son équivalent série décocherait deux cent dix-huit épisodes sans confirmation, et ce geste garde son seul chemin nommé (« Tout décocher », en tête de la liste des épisodes) ; par-dessus v103 — RETOUR-08 §1 : le bouton 💌 « recommander » est disponible sur CHAQUE fiche de série, quel que soit son état. Il était accroché à la rangée d'actions de « marquer l'épisode suivant comme vu » : une série TERMINÉE (le cas Blacklist, 218/218), une série EN PAUSE ou une série pas encore commencée n'avaient donc ni bouton ni bandeau — et la première est justement celle qu'on recommande le plus, puisqu'on vient de la finir. Le bandeau « Recommandée à X · il l'a ajoutée ✓ » disparaissait avec, si bien qu'une série déjà recommandée ne le disait nulle part une fois terminée. Recommander ne dépend ni de la progression, ni de la pause, ni du statut : la fiche film ne l'a jamais conditionné, l'aperçu non plus, la série était la seule des trois à le faire. Le bouton rejoint l'action principale en carré quand il y en a une, et prend la pleine largeur avec son libellé quand il n'y en a pas ; par-dessus v102 — RETOUR-07 §1 : l'écran Réglages › Notifications porte enfin un INTERRUPTEUR, toujours visible, qui coupe autant qu'il active — jusqu'ici il n'existait aucun chemin pour arrêter les notifications depuis l'app, et quand tout marchait l'écran ne proposait aucun réglage ; couper retire réellement l'appareil de `push_appareils` (ce n'est pas un filtre local), garde les cloches allumées, fait taire l'invitation du centre 🔔, survit au démarrage, à la synchro et à un changement de compte sur le même téléphone, et ne voyage pas d'un appareil à l'autre — c'est CE téléphone qu'on fait taire ; dans les trois cas où rien ne peut être activé (navigateur sans push, onglet Safari sur iPhone, notifications refusées au niveau du système) l'interrupteur se montre grisé avec la raison écrite dessous, au lieu de disparaître ; RETOUR-03 point 2 : la carte écrite de la pile de navigation en tête d'app-03-vues.js, et un contrôle statique qui la confronte au code à chaque lancement des tests ; par-dessus v101 — RETOUR-06 en entier : la Recherche affiche ses premières affiches sans attendre le reste (mesuré CPU bridé : 567 ms d'écran vide avant, 31 ms après), les pages qui manquent partent ensemble au lieu de se suivre en file, et refaire la même recherche ne redemande plus rien au réseau ; dans le centre de notifications la jaquette et le titre d'une reco ouvrent la fiche, et le retour — flèche comme balayage — ramène au centre puis à l'onglet d'origine ; et les seuils de popularité tombent pour TOUTES les catégories et plus seulement pour les animés — les dix-huit recettes d'ambiance perdent le plancher de votes qu'elles portaient en dur, jusqu'à 5 000 votes pour « Un classique que j'ai raté », et les séries rejoignent les animés au socle de 20 ; mesuré le 29/08 contre le vrai catalogue, chaque ambiance rend entre deux et neuf fois plus de titres (« Ça fait peur » passe de 469 à 4 408) ; par-dessus v99 — RETOUR-05 : les seuils de popularité tombent pour TOUTES les catégories et plus seulement pour les animés — les dix-huit recettes d'ambiance de la Recherche (films et séries) perdent le plancher de votes qu'elles portaient chacune en dur, jusqu'à 5 000 votes pour « Un classique que j'ai raté », et les séries rejoignent les animés au socle de 20 ; mesuré le 29/08 contre le vrai catalogue, chaque ambiance rend entre deux et neuf fois plus de titres (« Ça fait peur » passe de 469 à 4 408) ; par-dessus v99 — RETOUR-05 : activer les notifications ne demande plus de chercher — l'écran Réglages › Notifications porte enfin un bouton « Activer les notifications » (la question du téléphone ne se posait qu'en allumant la cloche d'un titre, et sur cinq comptes un seul avait trouvé le chemin), et le centre 🔔 propose la même activation en tête de fil tant que le push n'est pas actif, avec un « Plus tard » qui la fait taire trente jours sur tous tes appareils ; par-dessus v98 — RETOUR-04 et SPEC-10 : plusieurs genres cochés peuvent se demander « les deux ensemble » et plus seulement « au moins un », le plancher de votes saute dès qu'une plateforme est filtrée (les catalogues Netflix, Prime, Disney+, Canal+, Apple TV+, Max, Crunchyroll et ADN reviennent en entier) et descend à 20 sur les animés (Medalist et les autres animés sous-votés sur TMDB réapparaissent), les recommandations entre amis quittent « Mes abonnements » pour un centre de notifications derrière une cloche 🔔 posée sur Découvrir, En cours et Mon profil — qui compile les recos reçues, les retours sur les tiennes et les sorties, avec lu/non-lu synchronisé — et l'envoi se fait depuis un bouton 💌 de la fiche, à plusieurs amis à la fois et avec un mot ; par-dessus v97 — les retours de prod (RETOUR-01 et RETOUR-02) : les puces de Découvrir quittent l'affiche, « Ajuster mes goûts » disparaît, le « Top 10 pour toi » propose enfin des titres non vus, l'IA est active par défaut et son pitch suit chaque changement de hero, un bouton ✦ teinte la barre de Recherche pour chercher par envie, le tri « mes goûts » appelle réellement l'IA, la carte duel du profil passe en éventail avec sa famille au choix, la bibliothèque gagne une puce Animés, « Je ne l'ai pas vu » déménage dans la fiche ⓘ du duel et la recherche d'acteurs s'ouvre en plein écran — par-dessus v96, SPEC-07 (design premium : verre et profondeur, affiches, typographie, accent bijou — app.css seul), par-dessus SPEC-06 (le Duel accessible : bandeau du jour, duel éclair, carte et panthéon dans Mon profil, podium partageable, « je ne l'ai pas vu » rebranché et réversible), par-dessus SPEC-05 lot B (l'IA de la Recherche : envie en français, ambiance décrite, « pourquoi il te correspond »), par-dessus le lot A (Recherche nouvelle génération : pilules, feuille Filtres en accordéon, ambiances, meilleur match, tri « mes goûts » et mode duo), par-dessus SPEC-04 lot C (les branchements IA de Découvrir : lot quotidien, humeurs, interrupteurs de Réglages, « Bientôt » personnel), par-dessus le lot B (le relais IA) et le lot A (la règle des 10, les quatre humeurs, cinq rangées de plus).
